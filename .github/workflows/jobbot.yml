name: jobbot

on:
  schedule:
    - cron: "*/20 * * * *"   # держит следующий запуск в очереди: как только текущий закончится, он стартует через ~минуту
  workflow_dispatch: {}       # кнопка «Run workflow» — запустить вручную

concurrency:
  group: jobbot               # два запуска одновременно не пойдут — второй ждёт в очереди и стартует сразу после первого
  cancel-in-progress: false

jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 358       # максимум GitHub — 6 часов
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - run: pip install -r requirements.txt

      # База (что уже видели, кому уже писали) живёт в кэше GitHub между запусками
      - name: Restore state
        uses: actions/cache/restore@v4
        with:
          path: data
          key: jobbot-state-${{ github.run_id }}
          restore-keys: jobbot-state-

      - name: Run one pass
        env:
          DATA_DIR: data
          OWNER_ID: ${{ secrets.OWNER_ID }}
          TG_API_ID: ${{ secrets.TG_API_ID }}
          TG_API_HASH: ${{ secrets.TG_API_HASH }}
          TG_SESSION: ${{ secrets.TG_SESSION }}
          BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          JOBBOT_PROFILE: ${{ secrets.JOBBOT_PROFILE }}
        run: python main.py --for 345   # ~5 ч 45 мин, потом выходим и сохраняем базу

      - name: Save state
        if: always()
        uses: actions/cache/save@v4
        with:
          path: data
          key: jobbot-state-${{ github.run_id }}
